# Devin credentials come from the shell or the root .env (compose reads it);
# Settings refuses to load without them, so every backend service needs them
x-backend-env: &backend-env
  DATABASE_URL: postgresql+psycopg://devin:devin@db:5432/devin_flow
  DEVIN_API_TOKEN: ${DEVIN_API_TOKEN:?set DEVIN_API_TOKEN in .env}
  DEVIN_ORG_ID: ${DEVIN_ORG_ID:?set DEVIN_ORG_ID in .env}

services:
  db:
    image: postgres:18
    environment:
      POSTGRES_USER: devin
      POSTGRES_PASSWORD: devin
      POSTGRES_DB: devin_flow
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U devin -d devin_flow"]
      interval: 2s
      timeout: 5s
      retries: 15

  # one-shot alembic upgrade head, gates app and seed so only one migrates
  migrate:
    build: .
    entrypoint: ["alembic", "-c", "/app/backend/alembic.ini", "upgrade", "head"]
    environment:
      <<: *backend-env
    depends_on:
      db:
        condition: service_healthy

  # entrypoint re-runs alembic (a no-op here), then uvicorn on :8000
  app:
    build: .
    environment:
      <<: *backend-env
    ports:
      - "8000:8000"
    depends_on:
      migrate:
        condition: service_completed_successfully

  # one-shot: docker compose run --rm seed
  seed:
    build: .
    entrypoint: ["seed"]
    environment:
      <<: *backend-env
    depends_on:
      migrate:
        condition: service_completed_successfully
    profiles: [tools]

volumes:
  pgdata:
